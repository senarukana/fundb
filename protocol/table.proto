package protocol;

import "shard.proto";

message Table {
    required string name = 1;
    repeated string columns = 2;
    required int64 records = 3;
    required int64 size = 4;
    required bytes idtype = 5;
    optional int64 nextid = 6;
    repeated Shard shards = 7;
}